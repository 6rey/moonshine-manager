# 🚀 Установка admin_sunshine.py на новом ПК Windows

## Требования

1. **Python 3.8+** - [Скачать](https://www.python.org/downloads/)
2. **Moonlight** - для подключения к Sunshine хостам

## Установка

### Шаг 1: Установите Python

1. Скачайте Python с официального сайта
2. При установке **обязательно отметьте** "Add Python to PATH"
3. Перезагрузите компьютер после установки

### Шаг 2: Установите зависимости

Откройте PowerShell или CMD в папке проекта и выполните:

```powershell
# Установка основных зависимостей
pip install customtkinter
pip install requests
pip install PyJWT
pip install urllib3

# Установка zeroconf для mDNS (опционально, но рекомендуется)
pip install zeroconf
```

**Или одной командой:**

```powershell
pip install customtkinter requests PyJWT urllib3 zeroconf
```

### Шаг 3: Установите Moonlight (опционально)

Если планируете подключаться к VM:

1. Скачайте Moonlight с [GitHub](https://github.com/moonlight-stream/moonlight-qt/releases)
2. Установите в `C:\moonlight\` или обновите путь в коде:
   ```python
   MOONLIGHT_EXEC = "C:/moonlight/Moonlight.exe"  # Измените если нужно
   ```

## Запуск

### Вариант 1: Из командной строки

```powershell
cd путь\к\проекту\client
python admin_sunshine.py
```

### Вариант 2: Двойной клик

1. Создайте ярлык `.bat` файла:
   ```bat
   @echo off
   cd /d %~dp0
   python admin_sunshine.py
   pause
   ```
2. Сохраните как `run_admin.bat` в папке `client\`
3. Запускайте двойным кликом

## Первый запуск

1. **Логин:**
   - Введите admin логин/пароль
   - Настройте API URL если нужно (чекбокс "Show API URL field")

2. **Проверьте вкладки:**
   - Users
   - Virtual Machines
   - Assignment
   - VM Connection
   - ➕ Add VM (новая!)

3. **Настройте подсети** (для Auto-Discovery):
   - Перейдите в "➕ Add VM"
   - Нажмите "⚙️ Configure Subnets"
   - Добавьте ваши подсети в CIDR нотации
   - Сохраните

## Структура файлов

```
Eclypse_The-Sunshine-Manager/
├── client/
│   ├── admin_sunshine.py      ← Запускать этот файл
│   ├── subnets.conf           ← Создастся автоматически
│   └── client_config.txt      ← API URL (создастся автоматически)
└── server/
    └── main.py                 ← Должен быть запущен отдельно
```

## Возможные проблемы

### ❌ ModuleNotFoundError: No module named 'customtkinter'

**Решение:**
```powershell
pip install customtkinter
```

### ❌ ModuleNotFoundError: No module named 'zeroconf'

**Решение:**
```powershell
pip install zeroconf
```
Или запускайте без mDNS (будет только Port Scan)

### ❌ SSL Certificate Error

**Решение:**
- Снимите галочку "Verify SSL certificates" при логине
- Это нормально для self-signed сертификатов

### ❌ Connection refused / API не отвечает

**Проверьте:**
1. Сервер запущен? (`python server/main.py`)
2. Правильный API URL?
3. Firewall не блокирует?

## Минимальная конфигурация

**Для базовой работы достаточно:**
- Python 3.8+
- customtkinter
- requests
- PyJWT

**Для полной функциональности:**
- Все выше + zeroconf
- Moonlight (для подключений)

## Быстрый старт (одна команда)

```powershell
# 1. Установка зависимостей
pip install customtkinter requests PyJWT urllib3 zeroconf

# 2. Запуск
python admin_sunshine.py
```

---

**Готово!** 🎉 Теперь можно запускать `admin_sunshine.py`
